# spec-sheet-ingest

## 目标
把用户提供的参数表、价格表、产品资料包导入知识链。

## 什么时候用
- 用户上传文件、粘贴资料、提供 URL
- 本轮明显缺事实，且外部搜索可信度不够

## 执行顺序
1. 保存原始资料
2. 提取文本并切块
3. 生成结构化候选知识
4. 写入向量缓存与候选知识层
5. 送入会话审查

## 输出要求
- 不直接变成正式知识
- 返回导入摘要、候选知识数量、关键字段覆盖情况

## 失败回退
- 返回可见错误原因，并建议可接受的输入格式
